import { CalendarWidget } from "@/components/CalendarWidget";

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-4 md:p-8 bg-slate-200">
      <CalendarWidget />
    </main>
  );
}
